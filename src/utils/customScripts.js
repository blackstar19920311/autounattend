// Hardened 25H2 build: deployment-time PowerShell and credential-bearing scripts were removed.
// Enterprise software, updates, domain join and Wi-Fi profiles must be delivered by Intune,
// Autopilot, Configuration Manager, or another managed provisioning system.
export const SCRIPTS = Object.freeze({});
